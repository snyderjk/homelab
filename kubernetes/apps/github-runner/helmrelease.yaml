apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: arc
  namespace: arc-systems
spec:
  interval: 1h
  chart:
    spec:
      chart: gha-runner-scale-set-controller
      version: ">=0.9.0"
      sourceRef:
        kind: HelmRepository
        name: arc
        namespace: arc-systems
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
