apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: keycloak-db
  namespace: keycloak
spec:
  instances: 1
  storage:
    size: 5Gi
    storageClass: synology-csi-iscsi-retain
  bootstrap:
    initdb:
      database: keycloak
      owner: keycloak
